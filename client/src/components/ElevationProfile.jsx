/**
 * ElevationProfile — bottom panel for elevation chart (Canvas 2D).
 * Phase 1: placeholder. Chart implementation in Phase 4.
 */

export function ElevationProfile() {
  return (
    <div className="h-32 bg-slate-900 border-t border-slate-700 flex items-center justify-center shrink-0">
      <p className="text-slate-500 text-sm">
        Elevation profile — Canvas 2D — Phase 4
      </p>
    </div>
  );
}
