/**
 * Viewport3D — center panel for 3D terrain rendering.
 * Phase 1: placeholder. MapLibre + deck.gl wired in Phase 3.
 */

export function Viewport3D() {
  return (
    <div className="flex-1 relative bg-slate-800 flex items-center justify-center">
      <div className="text-center text-slate-400">
        <div className="text-6xl mb-4">🏔️</div>
        <p className="text-lg font-medium text-slate-300">3D Terrain Viewport</p>
        <p className="text-sm mt-1">MapLibre GL + deck.gl — Phase 3</p>
      </div>
    </div>
  );
}
