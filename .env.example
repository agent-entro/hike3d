# Copy to .env and adjust as needed.

# Port the Express API server listens on.
PORT=3004

# Origin of the Vite dev server — used for CORS on the backend.
# Must match the port Vite actually runs on (see client/vite.config.js server.port).
CLIENT_ORIGIN=http://localhost:5173
